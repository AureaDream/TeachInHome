**完成发布订单功能的实现**
## 新BUG：编辑和删除按钮无交互**事件
初步推测：可能是t-icon交互事件冲突
结论：函数绑定错误
//修复：修改函数绑定

**优化订单详情的显示**
//删除订单详情相关的代码

**修复编辑订单按钮的问题**
## BUG：点击编辑按钮后弹出的窗口空白
常见问题
## 修复点
- 在 g:\WechatProject\3\miniprogram\pages\admin\admin.wxml 的所有 t-dialog 内，将
  - view class="dialog-content" 改为 view class="dialog-content" slot="content"
- 覆盖了以下弹窗：
  - 编辑用户、编辑订单、添加管理员、修改密码
- 这与 g:\WechatProject\3\miniprogram\pages\profile\profile.wxml 的用法保持一致（其自定义内容均使用 slot="content" ）。
## 为什么会空白
- TDesign 小程序的 t-dialog 自定义内容需要放在 slot="content" 里，否则不会渲染子节点，只显示空白区域。

————完成订单管理界面的开发————

**完成用户管理功能的初步实现**
# BUG：类型“string”上不存在属性
问题：使用动态属性键进行 setData 操作时，TypeScript 无法正确推断 string | AnyObject 类型。

# BUG：
出现以下问题：
## 1.[Component] property "theme" of "miniprogram_npm/tdesign-miniprogram/tag/tag" received type-uncompatible value: expected <String> but get null value. Use empty string instead.
//Tag组件theme属性接收null值问题

## 2.发起的如下数据库查询经自动检测发现以下问题
查询语句:

db.collection('orders').where({})
.orderBy('createTime', 'desc')
.get()
全量查询告警: 空查询语句可能需扫全表
//问题：在 loadOrderList 函数中，当没有任何筛选条件时（即 whereConditions 为空对象），查询会变成全量查询。

## 3.显示管理员身份验证失败
//问题：同一id对应不同role导致
修复：增强了 checkAdminStatus 函数的验证逻辑，支持多种管理员身份标识。
- loginType === 'admin' - 管理员登录类型
- role === 'admin' - 用户角色为管理员
- isAdmin === true - 管理员标识

**修复问题：一个_openid只能注册admin角色或user角色**

**完成用户端订单界面的开发**
- 增加“标签→年级”的筛选映射：
- all 不筛选年级
- primary → 小学
- middle → 初中
- high → 高中
- university → 大学
- 保留原有搜索与学科筛选，分页 skip+limit 逻辑不变。
## 加载流程：
- 在 onLoad/onShow/下拉刷新/加载更多 中调用 loadOrders 。
- loadOrders 会按标签选择年级、标题检索（模糊、忽略大小写）、学科筛选，按 createTime 倒序分页获取 orders 集合数据。
- 将结果合并到页面的 orders 列表，并控制 loading/hasMore 状态。

**出现问题：【发布于】文本后方，不显示发布时间**
## 初步判断：
- 可能是无法读取用户发布时间
- 可能是时间格式问题